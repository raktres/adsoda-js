/**
 * @file Describes ADSODA group
 * @author Jeff Bigot <jeff@raktres.net> after Greg Ferrar
 * @class NDSolid
 * @todo use it
 */

import { NDObject } from './ndobject.js'

class NDSolid extends NDObject {
  constructor (objects) {
    super('NDsolid')
    // this.objectList = new Set()
    // if (objects) this.objectList = objects
  }
}